meta {
  name: Remove Participant
  type: http
  seq: 1
}

delete {
  url: {{host}}/community/:community_id/participant/:participant_id
  body: none
  auth: inherit
}

params:path {
  participant_id: {{participant_id}}
  community_id: {{community_id}}
}

settings {
  encodeUrl: true
}
