meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{host}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "amelia_aperiam@yahoo.com",
    "password": "password"
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    let data = res.getBody();
    bru.setEnvVar("sessionId", data.sessionId);
  }
  
}

settings {
  encodeUrl: true
}
